name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main 

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Install Firebase CLI
      run: npm install -g firebase-tools

    - name: Authenticate with Firebase
      run: firebase login:ci --token ${{ 1//09-TQJyBws417CgYIARAAGAkSNwF-L9IrQGFiDV5STZCQvSyhYrE6jiD9-GAHYlUGIwXWYhdG0y59IngfcbUudie6G8uiDSHKUOs }}
    
    - name: Change to functions directory
      run: cd flutter_application_1/functions


    - name: Deploy to Firebase Hosting
      run: firebase deploy --only hosting
